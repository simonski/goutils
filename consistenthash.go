package goutils

/*
type Event struct {
	name        string
	probability float64 // the chance of this event occurring
}

type ConsistentHash struct {
	events []Event
	increment float
}

func (c ConsistentHash) New([]events Event) int {
	smallestPrecision = 0
	for index:=0;index<len(events; index++) {
		decimalPlaces := CountDecimalPlaces(events[index].probabiliity)
		if decimalPlaces > smallestPrecision {
			smallestPrecision = decimalPlaces
		}
	}
	smallestIncrement >> smallestPrecision
	return ConsistentHash{events, smallestIncrement}
}

// Get returns an Event
func (c ConsistentHash) Get(value float64) int {
	for index=0; index<100; index+=c.smallestIncrement

	return 0
}

func CountDecimalPlaces(v float64) int {
    s := strconv.FormatFloat(v, 'f', -1, 64)
    i := strings.IndexByte(s, '.')
    if i > -1 {
        return len(s) - i - 1
    }
    return 0
}
*/
